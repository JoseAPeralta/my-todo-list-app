const { prueba } = require('../js/index')

describe("Test validation", () => {
    test('should ', () => {
        const expected = 1
        const result = prueba(1)
        expect(expected).toBe(result)
    })
})